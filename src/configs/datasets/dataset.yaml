train:
  _target_: src.datasets.MainDataset
  data_root: ${data_root}
  data_url: https://disk.360.yandex.ru/d/9k_k6G6a03GURg
  index_dir: ${index_dir}
  name: train
  sr: 16000
  instance_transforms: ${transforms.instance_transforms.train}

val:
  _target_: src.datasets.MainDataset
  data_root: ${data_root}
  index_dir: ${index_dir}
  name: val
  sr: 16000
  instance_transforms: ${transforms.instance_transforms.inference}
  limit: 1000
