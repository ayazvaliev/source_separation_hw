defaults:
  - model: baseline
  - writer: wandb
  - metrics: example
  - datasets: dataset
  - dataloader: dataloader
  - transforms: transforms
  - _self_

data_root: "."
index_dir: "indexes"
aug_dir: "augs"

# Такая хитровыебнная конструкция нужна, чтобы иметь возможность разделять парамы на группы с включенным и выключенным weight_decay
# Если есть идеи сделать это чище - то било бы ахуено
optimizer:
  cls: torch.optim.AdamW
  weight_decay: 1e-2
  optimizer_config:
    lr: 3e-4

lr_scheduler:
  _target_: torch.optim.lr_scheduler.StepLR
  gamma: 0.9
  step_size: ${trainer.epoch_len}

loss_function:
  _target_: src.loss.ExampleLoss

trainer:
  log_step: 50
  n_epochs: 100
  epoch_len: 500
  gradient_accumulation: null
  use_jit: True # врубает торчскриптовский jit. Не работает с автокастом >:(
  device_tensors: ["audio_s1", "audio_s2", "audio_mix"] # which tensors should be on device (ex. GPU)
  custom_mix: True
  resume_from: null # null or path to the checkpoint dir with *.pth and config.yaml
  device: auto # device name or "auto"
  override: True # if True, will override the previous run with the same name
  monitor: "max test_MulticlassAccuracy" # "off" or "max/min metric_name", i.e. our goal is to maximize/minimize metric
  save_period: 5 # checkpoint each save_period epochs in addition to the best epoch
  early_stop: ${trainer.n_epochs} # epochs for early stopping
  save_dir: "saved"
  seed: 1
